# === Core runtime ===
flask>=3.0,<4
numpy>=1.26.4,<2.0
opencv-python>=4.8,<5

# === TensorFlow (auto-pick by OS/CPU) ===
# Apple Silicon (arm64) on macOS
tensorflow-macos>=2.14,<3 ; platform_system == "Darwin" and platform_machine == "arm64"
tensorflow-metal>=1.1 ; platform_system == "Darwin" and platform_machine == "arm64"

# All other platforms (Intel macOS / Windows / Linux)
tensorflow>=2.14,<3 ; platform_system != "Darwin" or platform_machine != "arm64"